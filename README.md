# bunをブンブンしてみようのコーナー

## 開発方法

### 必須環境

- Docker (docker-compose)

### コマンド

```bash
# コンテナ起動
docker-compose up -d
# コンテナ再起動
docker-compose restart
# コンテナ停止
docker-compose down
# コンテナログイン
docker-compose exec bun bash
```